To run tests **locally** run following command in the terminal:<br>
`gradle clean test -Denv=local`<br>

To run test **remotly** run following command in the terminal:<br>
`gradle clean test -Denv=remote`<br>
